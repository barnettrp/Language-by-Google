{
  "name": "language-by-google",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dev-server.js",
    "dev:vite": "vite",
    "build": "vite build && cp public/*.js dist/ && cp -r public/images dist/",
    "preview": "vite preview",
    "vercel-build": "vite build && cp public/*.js dist/ && cp -r public/images dist/",
    "lint": "eslint public/ --ext .js",
    "lint:fix": "eslint public/ --ext .js --fix",
    "format": "prettier --write public/"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^16.3.1",
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "prettier": "^3.6.2",
    "vite": "^5.0.0"
  }
}
